# Environment Variables (Optional)
# This project doesn't require any environment variables to run.
# All features work out of the box without configuration.

# Optional: Analytics (if you want to add Google Analytics)
# VITE_GOOGLE_ANALYTICS_ID=GA_MEASUREMENT_ID

# Optional: Site Configuration (if needed for future features)
# VITE_SITE_URL=https://opscorelabs.com
# VITE_SITE_NAME=OpsCore Labs
